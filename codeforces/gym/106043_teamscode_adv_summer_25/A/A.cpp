#include <bits/stdc++.h>
using namespace std;

const int INF = int(1e9);

void solve() {
    int x1,y1,x2,y2;
    cin >> x1 >> y1 >> x2 >> y2;
}

int main() {
    cin.tie(0)->ios::sync_with_stdio(0);
    int t = 1;
    cin >> t;
    while (t--) {
        solve();
    }
    cout << -INF << " " << -INF << " " << INF << " " << INF << '\n';
    return 0;
}
