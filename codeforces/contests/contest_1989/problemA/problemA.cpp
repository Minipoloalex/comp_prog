#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    while (n--) {
        int x, y;
        cin >> x >> y;
        if (y <= -2) cout << "NO" << endl;
        else cout << "YES" << endl;
    } 
    return 0;
}
