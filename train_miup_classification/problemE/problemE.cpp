#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(NULL);
    ios::sync_with_stdio(false);
    int n, c;
    cin >> n >> c;
    for (int i = 0; i < n; i++) {
        int xi;
        cin >> xi;
    }
    return 0;
}

