#include <bits/stdc++.h>
using namespace std;

int main() {
    int T; cin >> T;
    for(int c = 1; c <= T; c++) {
        int n;
        cin >> n;

        for (int i = 0; i < n; i++) {
            
        }
    }
    return 0;
}