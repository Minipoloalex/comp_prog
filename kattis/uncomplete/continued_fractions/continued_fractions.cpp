#include <bits/stdc++.h>
using namespace std;

typedef vector<int> vi;

int add(vi::iterator v1b, vi::iterator v1e, vi::iterator v2b, vi::iterator v2e) {

}
int sub(vi::iterator v1b, vi::iterator v1e, vi::iterator v2b, vi::iterator v2e) {

}
int mul(vi::iterator v1b, vi::iterator v1e, vi::iterator v2b, vi::iterator v2e) {

}
int div(vi::iterator v1b, vi::iterator v1e, vi::iterator v2b, vi::iterator v2e) {

}

int main() {
    int n1, n2;
    cin >> n1 >> n2;
    vi f1(n1);
    vi f2(n2);
    for (int i = 0; i < n1; i++) cin >> f1[i];
    for (int i = 0; i < n2; i++) cin >> f2[i];


    return 0;
}
